[tool.poetry]
name = "hireflow"
version = "0.1.0"
description = "HireFlow: local-first, privacy-focused job application automation"
authors = ["HireFlow Team"]

[tool.poetry.dependencies]
python = "^3.11"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
